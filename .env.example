# Database connection string
# For local development with docker-compose, this is set automatically
# For Dokploy, create a PostgreSQL database and use its connection string
DATABASE_URL=postgresql://user:password@host:5432/database

# Server port (optional, defaults to 3000)
PORT=3000

# Origin URL for CSRF protection (required for production)
# Set this to match your domain, e.g., http://toolshed.local
ORIGIN=http://toolshed.local
